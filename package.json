{
  "name": "poc",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap && npx lerna run build --scope=\"@poc/@(api)\" && npx lerna run build --scope=\"@poc/@(redux)\" && yarn add husky --dev",
    "clean": "npx lerna clean --y"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  },
  "npmClient": "yarn",
  "dependencies": {
    "@types/node": "^16.11.11",
    "typescript": "^4.5.2"
  }
}
